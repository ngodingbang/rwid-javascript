const person = {
  name: "Ian",
  age: 27,
  address: "Yogyakarta",
};

function showPersonAtConsole() {
  console.info(`Name: ${person.name}`);
  console.info(`Age: ${person.age}`);
  console.info(`Address: ${person.address}`);
}

showPersonAtConsole();
